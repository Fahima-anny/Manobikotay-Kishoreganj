// card 1 start 
function changeSlide(slideId) {
    // Hide all carousel items
    document.querySelectorAll('.carousel-item').forEach(function(item) {
      item.classList.add('hidden');
    });
    // Show the selected carousel item
    document.querySelector(slideId).classList.remove('hidden');
  }

// card 2 start 
function changeSlide2(slideId) {
    // Hide all carousel items
    document.querySelectorAll('.carousel-item2').forEach(function(item) {
      item.classList.add('hidden');
    }); 
    // Show the selected carousel item
    document.querySelector(slideId).classList.remove('hidden');
  }

// card 3 start 
function changeSlide3(slideId) {
    // Hide all carousel items
    document.querySelectorAll('.carousel-item3').forEach(function(item) {
      item.classList.add('hidden');
    });
    
    // Show the selected carousel item
    document.querySelector(slideId).classList.remove('hidden');
  }

// card 4 start 
function changeSlide4(slideId) {
    // Hide all carousel items
    document.querySelectorAll('.carousel-item4').forEach(function(item) {
      item.classList.add('hidden');
    });
    
    // Show the selected carousel item
    document.querySelector(slideId).classList.remove('hidden');
  }

// card 5 start 
function changeSlide5(slideId) {
    // Hide all carousel items
    document.querySelectorAll('.carousel-item5').forEach(function(item) {
      item.classList.add('hidden');
    });
    
    // Show the selected carousel item
    document.querySelector(slideId).classList.remove('hidden');
  }

// card 6 start 
function changeSlide6(slideId) {
    // Hide all carousel items
    document.querySelectorAll('.carousel-item6').forEach(function(item) {
      item.classList.add('hidden');
    });
    
    // Show the selected carousel items
    document.querySelector(slideId).classList.remove('hidden');
  }

// card 7 start 
function changeSlide7(slideId) {
    // Hide all carousel items
    document.querySelectorAll('.carousel-item7').forEach(function(item) {
      item.classList.add('hidden');
    });
    
    // Show the selected carousel item
    document.querySelector(slideId).classList.remove('hidden');
  }

// card 8 start 
function changeSlide8(slideId) {
    // Hide all carousel items
    document.querySelectorAll('.carousel-item8').forEach(function(item) {
      item.classList.add('hidden');
    });
    
    // Show the selected carousel item
    document.querySelector(slideId).classList.remove('hidden');
  }

// card 9 start 
function changeSlide9(slideId) {
    // Hide all carousel items
    document.querySelectorAll('.carousel-item9').forEach(function(item) {
      item.classList.add('hidden');
    });
    
    // Show the selected carousel item
    document.querySelector(slideId).classList.remove('hidden');
  }

// card 10 start 
function changeSlide10(slideId) {
    // Hide all carousel items
    document.querySelectorAll('.carousel-item10').forEach(function(item) {
      item.classList.add('hidden');
    });
    
    // Show the selected carousel item
    document.querySelector(slideId).classList.remove('hidden');
  }

// card 11 start 
function changeSlide11(slideId) {
    // Hide all carousel items
    document.querySelectorAll('.carousel-item11').forEach(function(item) {
      item.classList.add('hidden');
    });
    
    // Show the selected carousel item
    document.querySelector(slideId).classList.remove('hidden');
  }

// card 12 start 
function changeSlide12(slideId) {
    // Hide all carousel items
    document.querySelectorAll('.carousel-item12').forEach(function(item) {
      item.classList.add('hidden');
    });
    
    // Show the selected carousel item
    document.querySelector(slideId).classList.remove('hidden');
  }

// card 13 start 
function changeSlide13(slideId) {
    // Hide all carousel items
    document.querySelectorAll('.carousel-item13').forEach(function(item) {
      item.classList.add('hidden');
    });
    
    // Show the selected carousel item
    document.querySelector(slideId).classList.remove('hidden');
  }

// card 14 start 
function changeSlide14(slideId) {
    // Hide all carousel items
    document.querySelectorAll('.carousel-item14').forEach(function(item) {
      item.classList.add('hidden');
    });
    
    // Show the selected carousel item
    document.querySelector(slideId).classList.remove('hidden');
  }

// card 15 start 
function changeSlide15(slideId) {
    // Hide all carousel items
    document.querySelectorAll('.carousel-item15').forEach(function(item) {
      item.classList.add('hidden');
    });
    
    // Show the selected carousel item
    document.querySelector(slideId).classList.remove('hidden');
  }

// card 16 start 
function changeSlide16(slideId) {
    // Hide all carousel items
    document.querySelectorAll('.carousel-item16').forEach(function(item) {
      item.classList.add('hidden');
    });
    
    // Show the selected carousel item
    document.querySelector(slideId).classList.remove('hidden');
  }

// card 17 start 
function changeSlide17(slideId) {
    // Hide all carousel items
    document.querySelectorAll('.carousel-item17').forEach(function(item) {
      item.classList.add('hidden');
    });
    
    // Show the selected carousel item
    document.querySelector(slideId).classList.remove('hidden');
  }

// card 18 start 
function changeSlide18(slideId) {
    // Hide all carousel items
    document.querySelectorAll('.carousel-item18').forEach(function(item) {
      item.classList.add('hidden');
    });
    
    // Show the selected carousel item
    document.querySelector(slideId).classList.remove('hidden');
  }

// card 19 start 
function changeSlide19(slideId) {
    // Hide all carousel items
    document.querySelectorAll('.carousel-item19').forEach(function(item) {
      item.classList.add('hidden');
    });
    
    // Show the selected carousel item
    document.querySelector(slideId).classList.remove('hidden');
  }
